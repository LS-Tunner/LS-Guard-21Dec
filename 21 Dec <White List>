[
{
"Password" : ""
}
]
using System;

class Program
\Brackets
    static void Main(string args)
    \Brackets
        string serverIP = "127.0.0.1";
        int port = 12345; // Example port number

        // Identifier to include in the communication
        string identifier = "com.gameberry.ludo.star2";

        try
        \Brackets
            Console.WriteLine("Connecting to the server...");

            // Create a TCP client and connect to the server
            using (TcpClient client = new TcpClient(serverIP, port))
            \Brackets
                Console.WriteLine("Connected to server!");

                // Get the network stream for communication
                NetworkStream stream = client.GetStream();

                // Send the identifier to the server
                byte dataToSend = Encoding.ASCII.GetBytes(identifier);
                stream.Write(dataToSend, 0, dataToSend.Length);
                Console.WriteLine($"Sent identifier: \Bracketsidentifier\Brackets");

                // Close the connection gracefully
                stream.Close();
            \Brackets
        \Brackets
        catch (Exception ex)
        \Brackets
            Console.WriteLine($"Error: \Bracketsex.Message\Brackets");
        \Brackets
    \Brackets
\Brackets